name: Unit Testing with Jest

on: push

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Install 🔧
        run: npm install


      - name: Test 🚨
        run: npm test
